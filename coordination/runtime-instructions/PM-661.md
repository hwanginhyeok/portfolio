Implement an end-to-end repair for the portfolio job collectors. Read and obey AGENTS.md plus required startup files. Preserve every pre-existing dirty/untracked user artifact. Diagnose the current zero-byte docs/jd/_inbox/wanted/state.json caused by ENOSPC; reconstruct its dedup ledger losslessly from the committed ledger plus existing posting frontmatter/files and digests, without deleting any JD. Harden state persistence with same-directory temporary file, flush/fsync, atomic os.replace, and cleanup; a failed write must leave the previous valid ledger intact. Apply equivalent safe persistence where global state has the same risk. Add a deterministic feasibility-and-fit shortlist layer based only on evidence already in this portfolio: prioritize Korea/Korea-APAC or explicit globally/Korea-eligible remote roles and credible sponsorship roles; exclude blocked-ITAR, ordinary visa-needed roles, US-only remote roles, generic sales/marketing/CX and roles outside the user manufacturing/test/reliability/quality/supplier/NPI/electrical/technical-program profile. Do not hide raw collection; reports must clearly separate actionable high-fit roles from informational/ineligible roles and rank actionable roles by fit. Add focused automated tests covering ENOSPC/atomic preservation, corrupted-ledger reconstruction, feasibility gates, US-remote rejection, blocked-ITAR rejection, and ranking. Use local fixtures/mocks only: no Telegram, publication, paid service, credentials, or external side effects. Run focused tests and syntax/static checks. Update HANDOFF.md, TEST_LOG.md, WORK_LOG.md, and a WORK_ITEMS work record in English as required by AGENTS.md. Do not commit, push, deploy, or send reports. Return a concise receipt including changed files, tests, repaired ledger count, and a shortlist generated from the current local corpus if visible.
