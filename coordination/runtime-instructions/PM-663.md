Continue the rejected PM-661 work in the current dirty source tree; read AGENTS.md and required startup files. Preserve all pre-existing and PM-661 data. Own and verify every helper module. Tighten shortlist quality: an actionable role must have profile-fit evidence in the title/department (not merely incidental body text), except a narrowly justified testable rule; add minimum fit gating so generic roles such as Order Management, sales, CX, marketing, generic AI/software roles, and unrelated manager roles cannot surface. Keep raw collection intact. Feasibility must remain conservative: blocked ITAR/U.S.-person always excluded; visa-needed excluded; sponsorship-likely actionable only with explicit credible sponsorship text; remote actionable only with explicit worldwide/Korea/APAC scope, not U.S.-only or unclear scope. Add negative and realistic corpus regression tests for false positives. Repair ownership/receipt coverage for scripts/shortlist.py, scripts/state_utils.py, scripts/wanted_state_repair.py. Remove generated tests/__pycache__ files. Add a safe --hydrate-zero-byte mode to the Wanted repair tool or equivalent bounded mechanism, then use network reads only (no Telegram or publication) to re-fetch the 16 zero-byte Wanted JD files; never delete a JD and report exact successes/failures. Run the Wanted collector once without --telegram only if needed to validate normal operation after repair, and prevent any duplicate or external message. Run focused tests, syntax checks without leaving bytecode, ledger integrity checks, and produce a concise top shortlist from the local Wanted+global corpus with title/company/location/eligibility/fit and direct source URL. If no role passes strict gates, say so rather than widening criteria. Update HANDOFF.md, TEST_LOG.md, WORK_LOG.md, and the existing WORK_ITEMS record in English. Do not commit, push, deploy, alter cron, or send Telegram/email.
