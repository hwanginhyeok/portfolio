# PORTFOLIO-20260824-02

## Goal

Add a separate AI-native and engineering-consulting collection lane without
weakening the existing manufacturing/test/electrical shortlist or feasibility
gates.

## Implemented

- Added `core`, `ai-native`, and `engineering-consulting` track metadata to
  Wanted/global search candidates, frontmatter, state rows, and reports.
- Added focused industrial AI, Physical AI, robotics/automation, simulation,
  forward-deployed engineering, technical consulting, solutions, AI product/PM,
  and transformation terms.
- Removed the global `solutions architect` exclusion and kept generic
  cloud/software/data roles informational through deterministic title/department
  signals plus physical/industrial owner-domain evidence.
- Preserved legacy rows by defaulting missing track fields to `core`.

## Verification

`PYTHONDONTWRITEBYTECODE=1 python3 -B -m unittest discover -s tests -p
'test_job_collectors.py' -v` — 25 tests passed.

AST parsing, JSON parsing, report fixtures, and `git diff --check` passed. No
collector run, Telegram delivery, state/JD regeneration, commit, or external
publication was performed.
